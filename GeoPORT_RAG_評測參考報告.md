# GeoPORT Visual GraphRAG PoC 評測參考報告

## 1. 長摘要

本報告整理 GeoPORT 災害調查 PDF 在 Visual GraphRAG 系統中的 PoC 評測結果，作為成果簡報前的參考資料。測試資料包含 `geoport.pdf` 與 `geoport2.pdf`，兩份文件皆為台2線70.1K / 70k+100 平浪橋南側崩塌事件之 GeoPORT 分析報告，內容涵蓋投影片、照片、地形圖、剖面圖、崩塌機制、雨量資訊、歷史災害紀錄與現場調查資料。

本次評測的重點不是宣稱正式產品準確率，而是確認 PoC 階段是否具備可用的資料解析、檢索、圖譜推理與回答生成能力。由於測試 PDF 屬於投影片式與圖像型文件，若僅使用 PyMuPDF 文字層抽取，取得內容多為頁碼、URL 與標題，無法支撐有效問答。因此，本案以 VLM 解析作為主要入庫方式，逐頁抽取頁面摘要、關鍵事實、圖像證據、建議節點與建議關係。

VLM 入庫後，`geoport.pdf` 解析 14 頁、產生 183 個 chunks；`geoport2.pdf` 解析 14 頁、產生 204 個 chunks；專案檔案統計顯示合計約 443 個 chunks。相較純文字層抽取，VLM 能明顯補足照片、剖面圖、UAV、DOD 與地質機制等資訊，是此類災害報告能否被 RAG 系統有效使用的關鍵。

本階段建立 40 題 PoC 評測題庫，每類 5 題，涵蓋位置、數值、時間、因果鏈、圖像判讀、跨頁整合、領域外與矛盾問題。整體結果顯示，系統已能回答部分核心問題，例如事件地點、道路里程、降雨資訊、崩塌機制與部分地質因果鏈；對領域外問題也大致能觸發警示。然而，在圖像細節、時間判讀、數值精準回答與跨頁整合方面仍不穩定。

就目前表現而言，系統不能簡化宣稱為「準確率 51.62%」或「準確率 65%」。較正確的說法是：在 PoC 題庫上，`source_page_hit` 約為 0.65，代表多數題目可命中至少一個預期來源頁；`answer_point_coverage` 約為 0.5162，代表答案重點仍有明顯漏點；`relation_hit` 約為 0.725，表示圖譜關係已有初步幫助；`anomaly_match` 約為 0.825，表示領域外與矛盾警示具備初步可用性。

本案的 Recurrent Attention Network 可定位為「以知識圖譜權重為基礎的遞迴注意力推理框架」，而不是現階段已訓練完成的神經網路模型。現有 GraphRAG 中的節點權重、邊權重、語意相似度、來源頁面與異常訊號，可視為 attention 在知識圖譜上的初步分配。查詢時，系統先根據使用者問題形成初始 attention，再沿著高權重節點與關係擴展證據，並根據新取得的來源頁、節點與關係更新權重，形成 recurrent reasoning loop。

需要特別說明的是，後續若導入類神經網路訓練，RAN 的主要角色並不是取代 LLM 直接回答，而是協助 LLM 找對資料、挑對證據與走對圖譜路徑。也就是說，LLM 仍是最後的回答生成器；RAN 則更像前段的注意力與證據選擇器。訓練後的 RAN 可學習在不同問題下哪些節點、關係、頁面與 chunks 應被提高權重，讓送入地端 LLM 的 context 更精準，進而提升回答正確性與可追溯性。

因此，短期不建議直接微調地端 LLM。本階段更合理的優先順序是：先擴充題庫與標註來源頁、答案重點、圖譜節點與關係路徑；再改善 VLM 解析、檢索、reranker、圖譜關係與回答 prompt；最後才依錯誤類型評估是否訓練 embedding、reranker、relation extractor 或 graph attention module。當這些前段證據品質穩定後，再考慮是否微調地端 LLM，會比直接訓練 LLM 更可控。

建議簡報可採用以下定位：

> 本系統以 Visual GraphRAG 為基礎，將 PDF 中的文字、圖像、地點、時間、數值與事件關係轉換為知識圖譜。圖譜中的節點與邊被賦予權重，用以表示其對使用者問題的重要程度。查詢時，系統會根據問題語意、圖譜關係與來源頁面反覆更新注意力分布，逐步聚焦於高相關證據，因此可視為一種 graph-weighted recurrent attention reasoning 機制。後續若導入 RAN 訓練，其目標會是改善 LLM 前段的證據選擇，而不是直接取代 LLM 生成答案。

## 2. 評分細節

### 2.1 測試資料與入庫方式

| 項目 | 內容 |
|---|---|
| 測試文件 | `geoport.pdf`、`geoport2.pdf` |
| 文件類型 | 投影片式、圖像型災害調查 PDF |
| 主要解析方式 | VLM 逐頁解析 |
| 評測題庫 | 40 題，每類 5 題 |
| 題庫位置 | `backend/evals/geoport_vlm_cases.jsonl` |

本次比較三種入庫方式：

| 方式 | 說明 | 評測觀察 |
|---|---|---|
| PyMuPDF 文字層 | 僅讀取 PDF 文字層。 | 內容多為頁碼、URL、標題，不適合此類圖像型 PDF。 |
| OCR 對照組 | 使用 OCR 抽取頁面文字後，以手動節點方式入庫。 | 可讀到部分主要文字，但圖像語意與結構化關係有限。 |
| VLM 解析 | 使用 VLM 逐頁解析投影片影像。 | 最適合本案，可抽取圖像、剖面、地質機制與建議關係。 |

VLM 入庫結果如下：

| 文件 | 頁數 | chunks |
|---|---:|---:|
| `geoport.pdf` | 14 | 183 |
| `geoport2.pdf` | 14 | 204 |
| 合計 | 28 | 443 |

### 2.2 評測指標

目前採用 deterministic PoC 指標，適合用來做版本比較與問題定位，但不等同正式產品準確率。

| 指標 | 說明 | 解讀方式 |
|---|---|---|
| `source_page_hit` | 是否至少檢索到一個預期來源頁面。 | 衡量系統有沒有抓到相關證據。 |
| `source_page_recall` | 預期來源頁面被找回的比例。 | 衡量跨頁或多證據題是否找齊來源。 |
| `answer_point_coverage` | 回答是否包含預期重點。 | 衡量答案內容完整度。 |
| `relation_hit` | 是否命中預期圖譜關係或關係語意。 | 衡量 GraphRAG 關係是否有輔助。 |
| `anomaly_match` | 領域外或矛盾警示是否符合預期。 | 衡量拒答、警示與矛盾偵測能力。 |

補充：本階段已加入基本正規化，例如 `57.5 mm`、`57.5毫米`、`57.5公釐` 會視為相近表述，以降低格式差異造成的低估。

### 2.3 解析方式比較結果

| 入庫方式 | source_page_hit | answer_point_coverage | anomaly_match | 解讀 |
|---|---:|---:|---:|---|
| PyMuPDF 文字層 | 0.20 | 0.2667 | 1.00 | 不適合此類圖像型 PDF，主要內容未被抽取。 |
| OCR 對照組 | 1.00 | 0.6167 | 1.00 | 可讀到主要文字，但圖像語意與結構化關係有限。 |
| VLM 解析 | 1.00 | 0.6667 | 1.00 | 明顯最佳，可抽取圖像、剖面與地質語意。 |

結論：此類投影片式災害調查 PDF 必須使用 VLM 解析；若僅使用文字層抽取，系統問答表現會顯著下降。

### 2.4 40 題 VLM 題庫整體結果

| 指標 | 分數 | 初步解讀 |
|---|---:|---|
| `source_page_hit` | 0.6500 | 多數題目可命中至少一個預期來源頁，但仍有漏抓。 |
| `source_page_recall` | 0.5833 | 多頁證據找回不完整，跨頁題仍需改善。 |
| `answer_point_coverage` | 0.5162 | 答案重點約半數被涵蓋，完整度仍不足。 |
| `relation_hit` | 0.7250 | 圖譜關係已有幫助，但關係方向與鏈結仍需強化。 |
| `anomaly_match` | 0.8250 | 領域外與矛盾警示大致可用。 |

### 2.5 題型與回答結果

| 題型 | 題數 | 評測目的 | 範例題目 | 目前回答結果 |
|---|---:|---|---|---|
| 位置 | 5 | 測試地點、座標、坡向、受影響設施等資訊檢索。 | 崩塌事件位於哪一條道路與里程位置？ | 來源命中穩定；部分座標、坡向細節會漏答。 |
| 數值 | 5 | 測試雨量、岩塊尺寸、面積、量體、強度等數值回答。 | 報告中提到的累積雨量或土方量是多少？ | 簡單數值可答；面積、量體、強度仍不穩。 |
| 時間 | 5 | 測試事件時間、報告日期、歷史災害時間序列。 | 本次崩塌與現地調查分別發生在什麼時間？ | 日期與事件時間容易混淆，是弱項之一。 |
| 因果鏈 | 5 | 測試崩塌機制與地質因果鏈回答。 | 從降雨到坡趾崩落與傾覆破壞的因果鏈為何？ | 表現較佳，可涵蓋主要破壞機制。 |
| 圖像判讀 | 5 | 測試照片、剖面圖、UAV、DOD、現場調查圖像資訊。 | UAV 或 DOD 圖中可觀察到哪些地形變化？ | 目前最弱；圖像細節與 DOD 解讀需強化。 |
| 跨頁整合 | 5 | 測試跨頁整合多個證據來源的能力。 | 綜合歷史災害、雨量與剖面圖，主要風險因子有哪些？ | 常只命中部分頁面，回答完整性不足。 |
| 領域外 | 5 | 測試與報告無關問題是否能警示。 | 台北捷運票價調整對本崩塌事件有何影響？ | 大致可用，多數無關問題能觸發警示。 |
| 矛盾問題 | 5 | 測試錯誤因果方向或與圖譜關係不一致的問題。 | 岩橋斷裂導致降雨發生，是否符合因果關係？ | 可初步偵測；仍需強化關係方向判斷。 |

### 2.6 目前限制

1. **題庫規模仍小**
   - 40 題足以支撐 PoC 展示，但不足以代表正式產品效能。

2. **ground truth 尚未由領域專家審定**
   - 預期答案依 VLM 解析內容與人工檢視建立，尚未經地工或防災專家逐題確認。

3. **答案評分仍偏字面**
   - 雖已加入單位正規化，但同義詞、語意等價與部分正確回答仍可能被低估。

4. **圖像判讀依賴 VLM 解析品質**
   - 若 VLM 對照片、剖面圖、DOD 圖或 UAV 圖判讀不完整，後續 RAG 回答也會受限。

5. **GraphRAG 關係評估仍粗略**
   - 目前 relation_hit 只能初步判斷是否命中關係，尚未完整評估關係方向、因果鏈完整性與圖譜一致性。

6. **尚未導入 faithfulness / groundedness 評估**
   - 目前尚未系統性量化回答是否完全被檢索內容支撐。

## 3. 未來建議

### 3.1 短期：先強化解析、檢索與回答控制

1. **固定使用 VLM pipeline**
   - 對投影片型、圖像型、掃描型 PDF，應將 VLM 設為主要解析方式。
   - PyMuPDF 文字層僅適合文字型 PDF，不應作為此類災害報告的主要入庫方式。

2. **頁面解析加入品質檢查**
   - 若某頁抽取結果只包含 URL、頁碼、標題，應自動標記為低品質，改走 VLM 或 OCR。

3. **圖像證據結構化**
   - 將 VLM 輸出的 `圖像證據`、`數值與位置`、`建議節點`、`建議關係` 拆成更明確的欄位，避免全部混在一般文本 chunk 中。

4. **針對圖表、剖面與照片加入專用 prompt**
   - 對剖面圖輸出岩層、弱面、坡度、破壞面、受影響道路。
   - 對 UAV / DOD 圖輸出面積、損失量體、堆積量體、日期、比例尺。

5. **加入 reranker 與 lexical fallback**
   - 在 Qdrant top-k 後加入 reranker，重排更精確的來源頁面。
   - 對日期、座標、尺寸、雨量、面積、量體等問題加入 keyword / lexical search。

6. **回答生成加入證據約束**
   - 回答中應列出來源文件與頁碼。
   - 數值題以表格或條列回答。
   - 若檢索內容不足，應回答「資料不足」，不應猜測。

### 3.2 中期：強化 GraphRAG 與 Recurrent Attention

1. **標準化 relation schema**
   - 將關係類型固定為 `位於`、`導致`、`促成`、`形成`、`影響`、`具有條件` 等。

2. **保留因果方向**
   - 因果關係必須明確保存 from/to，否則矛盾問題難以穩定判斷。

3. **建立因果鏈評分**
   - 不只判斷 relation 是否命中，也要評估「起點、中介、結果」是否完整。

4. **將圖譜權重對齊 Recurrent Attention**
   - 將節點權重視為 attention score。
   - 將邊權重視為 evidence transition score。
   - 查詢時先根據問題語意初始化權重，再沿高權重關係擴展候選證據。
   - 每輪擴展後重新計算節點與邊的重要度，形成「初始化 attention -> 圖譜擴展 -> 權重更新 -> 證據整合」的 recurrent attention loop。

5. **明確定位 RAN 對 LLM 的角色**
   - RAN 訓練後的主要目的，是幫助 LLM 找對資料、挑對證據、走對圖譜路徑。
   - LLM 仍負責最終回答生成；RAN 負責前段 attention 與 evidence selection。

可用流程描述如下：

```text
使用者問題
  -> RAN 判斷哪些節點 / 關係 / 頁面重要
  -> GraphRAG 依權重擴展與檢索證據
  -> 將更精準的 context 交給地端 LLM
  -> LLM 根據證據產生回答與來源引用
```

### 3.3 評測機制擴充

1. **題庫擴充至 80-100 題**
   - 每類題型至少 10 題，並加入更多跨文件、多頁、多證據問題。

2. **加入語意等價評分**
   - 目前 answer_point_coverage 偏字面，後續可加入 LLM-as-judge 判斷答案是否語意等價。

3. **加入 RAG 主流指標**
   - `context precision`
   - `context recall`
   - `faithfulness / groundedness`
   - `answer relevancy`
   - `answer correctness`

4. **錯誤類型標註**
   - 每題失敗應分類為 PDF/VLM 解析錯誤、retrieval miss、reranking miss、answer incomplete、hallucination、anomaly false positive / false negative、relation direction error。

5. **建立回歸測試**
   - 每次調整 prompt、chunking、relation extraction 或 reranker 後，應重跑同一題庫，確認表現是否提升。

### 3.4 類神經網路訓練建議

目前不建議直接微調地端 LLM。原因是目前多數錯誤並非 LLM 不會生成句子，而是前段證據選擇仍不完整：VLM 可能漏掉圖像細節、retrieval 可能找錯頁、graph 關係方向可能不清楚、cross-page evidence 可能不完整。若直接微調 LLM，可能無法解決「送進模型的資料不對」這個核心問題。

較建議的訓練優先順序如下：

| 可訓練項目 | 是否短期必要 | 用途 |
|---|---|---|
| LLM fine-tune | 否 | 改善回答格式、領域語氣與專業用語，但不一定解決找錯資料問題。 |
| embedding fine-tune | 中後期 | 改善災害報告語意檢索。 |
| reranker fine-tune | 較值得 | 將正確來源頁與 chunks 排到更前面。 |
| relation extractor | 較值得 | 改善因果、位置、影響等圖譜關係。 |
| graph attention module | 後期研究亮點 | 學習不同問題下節點與邊的權重分配。 |

若後續要真正導入 Recurrent Attention Network，可將目前 PoC 指標轉為訓練資料：

| 訓練訊號 | 可由現有評測延伸的標註 |
|---|---|
| 節點 attention | 每題應命中的地點、事件、數值、時間、機制節點。 |
| 邊 attention | 每題應通過的 `導致`、`位於`、`影響`、`形成` 等關係。 |
| recurrent step | 多輪擴展時每一輪應補入的來源頁或鄰近節點。 |
| evidence selection | 最終應進入回答 context 的頁面、chunk 與圖像證據。 |
| answer supervision | 預期答案重點、不可回答條件、矛盾警示標籤。 |

建議訓練流程：

```text
PoC 評測題庫
  -> 標註正確來源頁 / 正確節點 / 正確關係 / 預期答案
  -> 訓練 reranker 或 graph attention
  -> 改善送進地端 LLM 的證據品質
  -> 重新評測
  -> 再視需要微調地端 LLM
```

### 3.5 簡報可用結論

可放入成果簡報的結論如下：

> 本 PoC 已建立 40 題自動化評測題庫，涵蓋位置、數值、時間、因果鏈、圖像判讀、跨頁整合、領域外與矛盾問題。結果顯示，VLM 對圖像型 PDF 的解析是系統可用性的關鍵；相較純文字層抽取，VLM 可大幅提升來源頁面命中與回答完整性。目前系統已能回答核心地點、座標、降雨與崩塌機制問題，但在圖像細節、跨頁整合、時間與數值精準回答方面仍需透過 reranker、結構化 VLM 輸出、GraphRAG 關係標準化與更完整評測機制持續優化。

若簡報標題需呼應 **Recurrent Attention Network**，可補充：

> 本研究將知識圖譜中的節點與邊權重視為 attention 分布，並透過多輪檢索、圖譜擴展與證據回填形成 recurrent reasoning loop。現階段為 graph-weighted recurrent attention 的 PoC 雛型；後續 RAN 訓練的主要目標不是取代 LLM 回答，而是學習更精準的節點、關係與來源頁選擇，使地端 LLM 能基於更正確的 context 產生回答。
